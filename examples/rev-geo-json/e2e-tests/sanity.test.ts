import { test } from '@playwright/test';

test.describe('sanity', () => {
    test('sanity test', async ({ page }) => {
        // TODO: create custom sanity test for this example
        // await sanityE2ETest({ page, testInfo: test.info() });
    });
});
