const placesModule = await PlacesModule.get(map);
- label: "PRIVATE PREVIEW"
  color: "grey5"
const results = await search({

    position: [4.9041, 52.3676]

## Basic Usage
// Add places to map
placesModule.addPlaces({
    id: 'restaurants',
    places: results.results
});
import { TomTomMap, PlacesModule } from '@cet/maps-sdk-js/map';
import { search } from '@cet/maps-sdk-js/services';
## Adding Custom Places
const map = new TomTomMap({ container: 'map' });
// Initialize Places module with default configuration
// Add custom places with GeoJSON format
const customPlaces = [
    {
        type: 'Feature',
        geometry: {
            type: 'Point',
            coordinates: [4.9041, 52.3676]
        },
        properties: {
            name: 'My Custom Place',
            description: 'A special location'
        }
    }
];
```javascript
placesModule.addPlaces({
    id: 'custom-locations',
    places: customPlaces
const searchResults = await search({
    query: 'restaurants',
    center: [4.9041, 52.3676], // Amsterdam
## Place Styling

// Display results on map
placesModule.addPlaces({
    id: 'restaurants',
    places: results.results,
    style: {
        icon: 'restaurant',
        iconColor: '#FF0000',
        iconSize: 1.2
    }
    console.log('Place hovered:', feature.properties.name);
});
```
## Managing Places
### Custom Styling Configuration

// Remove places
placesModule.removePlaces('restaurants');
```
// Clear all places

```javascript
// Get place group IDs
const groupIds = placesModule.getPlaceGroupIds();

// Check if group exists
if (placesModule.hasPlaceGroup('restaurants')) {
    console.log('Restaurants are displayed');
}
});

## Place Interactions
restaurantModule.show(restaurantResults);
hotelModule.show(hotelResults);
// Listen for place clicks
map.getMaplibreMap().on('click', (event) => {
    const features = map.getMaplibreMap().queryRenderedFeatures(event.point, {
        layers: placesModule.getLayerIds()
    });

    if (features.length > 0) {
        const place = features[0];
        console.log('Place clicked:', place.properties.name);

// Add click handler for reverse geocoding
map.mapLibreMap.on('click', async (event) => {
    const result = await reverseGeocode({
## Places with Popups

### Performance Optimization
import maplibregl from 'maplibre-gl';
- **Handle errors gracefully**: Implement error handling for service calls
map.getMaplibreMap().on('click', (event) => {
    const features = map.getMaplibreMap().queryRenderedFeatures(event.point, {
        layers: placesModule.getLayerIds()
    });

    if (features.length > 0) {
        const place = features[0];

        new maplibregl.Popup()
            .setLngLat(place.geometry.coordinates)
            .setHTML(`
                <h3>${place.properties.poi?.name || place.properties.name}</h3>
                <p>${place.properties.address?.freeformAddress}</p>
            `)
            .addTo(map.getMaplibreMap());
    }
    zoom: 12
});

For complete examples, check the `/examples` folder in the repository.
// Initialize Places module
const placesModule = await PlacesModule.init(map, {
    markerConfig: { color: '#007cbf', size: 1.1 },
    popupConfig: { closeButton: true, maxWidth: '250px' }
});

// Search and display restaurants
const restaurants = await search({
    query: 'restaurants',
    center: [4.9041, 52.3676],
    radius: 2000,
    limit: 20
});

placesModule.show(restaurants);

// Handle interactions
placesModule.events.on('click', (feature, lngLat) => {
    console.log(`Clicked on: ${feature.properties.name}`);
    console.log(`Address: ${feature.properties.address}`);
});
```
