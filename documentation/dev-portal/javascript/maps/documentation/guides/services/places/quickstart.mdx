---
title: Places quickstart
hideMenu: false
hideSubmenu: false
hasTwoColumns: false
titleTags:
- label: "VERSION 0.9.3"
  color: "grey5"
- label: "PRIVATE PREVIEW"
  color: "grey5"
---

# Places Services Quickstart

The TomTom Places Services provide comprehensive location-based functionality including search, geocoding, and reverse geocoding capabilities.

## Basic Setup

```javascript
import { TomTomConfig } from '@cet/maps-sdk-js/core';
import { search, geocode, reverseGeocode } from '@cet/maps-sdk-js/services';

TomTomConfig.instance.put({
    apiKey: 'YOUR_API_KEY_HERE'
});
```

## Quick Examples

### Search for Places

```javascript
const results = await search({
    query: 'coffee shops',
    position: [4.9041, 52.3676] // Amsterdam
});

console.log(results.results[0].poi.name);
```

### Geocode an Address

```javascript
const result = await geocode({
    query: 'Dam Square, Amsterdam'
});

const coordinates = result.features[0].geometry.coordinates; // [lng, lat]
```

### Reverse Geocode Coordinates

```javascript
const result = await reverseGeocode({
    position: [4.9041, 52.3676]
});

console.log(result.features[0].properties.address.freeformAddress);
```

## Next Steps

- [Search Services](/javascript/maps/documentation/guides/services/places/search) - Autocomplete, fuzzy, and geometry search
- [Geocoding](/javascript/maps/documentation/guides/services/places/geocoding) - Address to coordinates
- [Reverse Geocoding](/javascript/maps/documentation/guides/services/places/reverse-geocoding) - Coordinates to address

For complete examples, check the `/examples` folder in the repository.
