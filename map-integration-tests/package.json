{
  "name": "map-integration-tests",
  "description": "Runs integration tests by starting a local web server with a page that contains the SDK code.",
  "scripts": {
    "test": "pnpm exec playwright install --with-deps chromium && pnpm exec playwright test",
    "test:coverage": "",
    "start-dev-server": "vite",
    "type-check": "tsc --noEmit"
  },

  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@tsconfig/recommended": "catalog:",
    "@types/geojson": "catalog:",
    "@types/lodash-es": "catalog:",
    "@types/node": "catalog:",
    "@vitejs/plugin-basic-ssl": "^2.1.0",
    "core": "workspace:*",
    "lodash-es": "catalog:",
    "map": "workspace:*",
    "maplibre-gl": "catalog:",
    "rimraf": "catalog:",
    "ts-node": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:"
  }
}
