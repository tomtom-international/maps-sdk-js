{
  "name": "map",
  "description": "TomTom Maps SDK JS - bundle for map display and interaction",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/map.es.js"
    }
  },
  "sideEffects": false,
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:dist": "vitest run dist-validation.test.ts",
    "build": "vite build",
    "build:watch": "vite build --watch",
    "build:full": "pnpm type-check && pnpm build",
    "type-check": "tsc --noEmit",
    "refresh-style-fixture": "node scripts/refresh-style-fixture.mjs",
    "clean:dist": "rimraf ./dist"
  },
  "devDependencies": {
    "@types/geojson": "catalog:",
    "@types/lodash-es": "catalog:",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "jsdom": "catalog:",
    "rimraf": "catalog:",
    "shared-configs": "workspace:*",
    "ts-node": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  },
  "typedoc": {
    "entryPoint": "./index.ts"
  },
  "peerDependencies": {
    "@turf/turf": "catalog:",
    "lodash-es": "catalog:",
    "maplibre-gl": "catalog:"
  },
  "dependencies": {
    "core": "workspace:*"
  }
}
