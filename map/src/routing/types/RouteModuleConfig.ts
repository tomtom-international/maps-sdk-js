/**
 * Parameters for the routing module.
 */
export type RoutingModuleConfig = {
    sections?: {
        traffic?: {
            visible?: boolean;
            icons?: {
                visible?: boolean;
            };
        };
    };
};
