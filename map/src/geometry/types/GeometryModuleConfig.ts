export type GeometryModuleConfig = {
    /**
     * Whether to display the polygons inverted (as a transparent map "hole" with opaque surroundings).
     * @default true
     */
    inverted?: boolean;
};
